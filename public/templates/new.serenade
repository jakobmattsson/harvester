div

  h3 "Required fields"
  - collection @required
    div
      span @title
      input[type="text" binding:keyup=@value]

  h3 "Optional fields"
  - collection @optional
    div
      span @title
      input[type="text" binding:keyup=@value]

  div
    button[event:click=send!] "submit"
